# HttpProxyChromeExtension

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 16.2.0.


## Build

Run ` npm run chrome-extension:build` to build the project. The build artifacts will be stored in the `dist/` directory.

